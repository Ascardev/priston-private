#pragma once

namespace SKY
{
	/*typedef class Handle *LPHANDLE;

	class Handle
	{
	public:
		Handle();
		virtual ~Handle();

		void Init();
		void Shutdown();

		void Render(Point3D sCameraPosition, Point3D sCameraAngle);
		void Update(float fTime);
		void SetSky(int iID);

		void Open(EMapSky eID);
	protected:
		void LoadFile(const char *pszFileName);
		Info *GetInfo(int iID);
	private:
		Info *m_pCurInfo = NULL;
		Info *m_pNexInfo = NULL;

		int m_iTimeCount = 0;
		int m_iBlinkColor = 0;

		std::vector<Info *> m_pvInfo;
	};*/
}